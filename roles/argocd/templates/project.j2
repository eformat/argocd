apiVersion: project.openshift.io/v1
kind: Project
metadata:
  annotations:
    openshift.io/description: "{{ _argocd_project_description }}"
    openshift.io/display-name: "{{ _argocd_project_display }}"
  name: "{{ argocd_project_name }}"
spec:
  finalizers:
  - kubernetes